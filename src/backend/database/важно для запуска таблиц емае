# 1. Инициализация
alembic init alembic

# 2. В alembic.ini укажи строку подключения:
# sqlalchemy.url = mysql+pymysql://root:root@localhost:3306/plant_game

# 3. В alembic/env.py замени:
# from myapp.db.models import Base
# target_metadata = Base.metadata

# 4. Создай миграцию и примени:
alembic revision --autogenerate -m "initial plant game schema"
alembic upgrade head